const { postRoom, allRooms, getRoomByType, getRoomById, updateRoom, deleteRoom } = require('./rooms.js')

module.exports = {
	postRoom,
	allRooms,
	getRoomByType,
	getRoomById,
	updateRoom,
	deleteRoom
}